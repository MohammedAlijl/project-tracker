# CLAUDE.md

تعليمات العمل على هذا المشروع. اقرأ `PRD.md` للمتطلبات الكاملة.

---

## عن المشروع

متتبّع مشاريع ومهام — لوحة كانبان بثلاثة أعمدة.

**التقنيات:** Next.js (App Router) · Supabase (PostgreSQL + Auth) · Cloudflare (نشر ونطاق)

---

## قواعد أساسية — لا تُخالَف

### ١. الـ Schema عبر migrations فقط

أي تغيير في قاعدة البيانات يكون بملف migration عبر Supabase CLI.

- ❌ لا تنفّذ `CREATE TABLE` أو `ALTER TABLE` مباشرة عبر MCP أو الـ Dashboard
- ✅ أنشئ ملفاً في `supabase/migrations/` باسم يبدأ بـ timestamp، ثم طبّقه

**Forward-only:** لا تعدّل migration مطبَّقة. التصحيح يكون بملف جديد.

### ٢. أسرار المشروع

- المفاتيح في `.env.local` فقط، وهو محجوب في `.gitignore`
- حدّث `.env.example` بأسماء المتغيّرات (بدون قيمها) عند إضافة أي متغيّر
- ❌ لا تكتب مفتاحاً في الكود ولو مؤقتاً
- `SUPABASE_SERVICE_ROLE_KEY` يُستخدم في الخادم فقط — يتجاوز RLS بالكامل

### ٣. الاستعلامات

استخدم معاملات دائماً. ممنوع دمج مدخلات المستخدم في نص SQL.

```js
// ✅
await pool.query('SELECT * FROM tasks WHERE project_id = $1', [projectId])

// ❌
`SELECT * FROM tasks WHERE project_id = ${projectId}`
```

### ٤. RLS مُفعَّل على كل جدول

كل جدول جديد يحتاج `ENABLE ROW LEVEL SECURITY` وسياساته في نفس الـ migration. جدول بلا سياسات = بيانات مكشوفة.

ملاحظة: `create policy` لا يدعم `if not exists` — استخدم `drop policy if exists` قبله.

---

## أسلوب العمل

**مرحلة واحدة في كل مرة.** المراحل في القسم ١١ من `PRD.md`. لا تنتقل لمرحلة قبل أن أؤكد اكتمال السابقة.

**قبل التنفيذ:** إذا كانت المهمة تتجاوز ملفين، اعرض خطة موجزة أولاً وانتظر موافقتي.

**بعد كل مرحلة:** اقترح رسالة commit ولا تدفع لـ GitHub قبل موافقتي.

**عند الغموض:** اسأل بدل أن تفترض. تفضيلاتي غير واضحة أحياناً، والسؤال أرخص من إعادة البناء.

---

## قواعد الكود

- TypeScript، ووضع strict
- أسماء الجداول والأعمدة `snake_case` — أسماء الكود `camelCase`
- مكوّنات الخادم افتراضياً، و `'use client'` عند الحاجة فقط
- لا تُنشئ ملفات غير مطلوبة. عدّل الموجود بدل إنشاء نسخة جديدة
- لا تُنشئ ملفات توثيق تلقائياً — إلا إذا طلبتها

---

## الـ API

المسارات تحت `app/api/`. راجع القسم ٨ في `PRD.md` للتفاصيل.

**رموز الحالة — استخدمها بدقة:**

| الرمز | متى |
|---|---|
| `200` | نجاح |
| `201` | إنشاء مورد |
| `400` | مدخلات غير صالحة |
| `401` | لا توجد جلسة |
| `403` | المورد ليس ملك المستخدم |
| `404` | غير موجود |

**معالجة النتائج — ثلاث حالات:**
1. قراءة فارغة → `[]` مع `200` (نجاح، ليس خطأ)
2. تعديل بلا تطابق → `rowCount = 0` (تحقّق منه صراحةً)
3. رفض من الـ driver → استثناء يحتاج `try/catch`

---

## قرارات تصميمية مقصودة

هذه القرارات مدروسة — لا "تحسّنها" بلا نقاش:

**لا نخزّن حالة التأخير.** تُحسب لحظة العرض:
```sql
due_date < CURRENT_DATE AND status <> 'done'
```

**لا نخزّن نسبة الإنجاز.** تُحسب باستعلام مجمّع.

**الترتيب بأرقام متباعدة.** `position` يبدأ بـ `10, 20, 30`. الإدراج بين اثنين يأخذ القيمة الوسطى بتحديث صف واحد فقط.

**الـ API على مسار `/api`** داخل نفس النطاق — لا نطاق فرعي. هذا يلغي الحاجة لـ CORS كلياً.

---

## نقاط انتبه لها

- عند إضافة عمود `NOT NULL` لجدول فيه بيانات: أضفه nullable ← عبّئه ← تحقّق ← فعّل القيد
- `auth.uid()` تُرجع `NULL` في الـ SQL Editor — اختبر السياسات بمستخدم فعلي
- اسم ملف الـ migration يجب أن يبدأ بـ timestamp وإلا يُتخطّى
- الحذف المتتالي مقصود: حذف مشروع يحذف مهامه عبر `ON DELETE CASCADE`

---

## معيار الاكتمال قبل النشر

- [ ] حسابان مختلفان لا يريان بيانات بعضهما — مُختبَر فعلياً
- [ ] كل تغيير في الـ schema موجود كملف في `supabase/migrations/`
- [ ] لا يوجد أي مفتاح في تاريخ المستودع
- [ ] الدورة الكاملة تعمل: تسجيل ← مشروع ← مهمة ← نقلها ← اكتمال
