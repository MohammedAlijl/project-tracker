# وثيقة متطلبات المنتج (PRD)
## متتبّع المشاريع والمهام

| | |
|---|---|
| **الإصدار** | 1.0 |
| **التاريخ** | أغسطس ٢٠٢٦ |
| **الحالة** | مسوّدة معتمدة للتنفيذ |

---

## ١. الملخّص التنفيذي

تطبيق ويب يتيح للمستخدم إدارة مشاريعه الشخصية الصغيرة عبر لوحة كانبان بثلاثة أعمدة. كل مستخدم يملك مشاريعه الخاصة، وكل مشروع يحتوي مهامَّ تنتقل بين حالات ثلاث: *لم يبدأ*، *جارٍ*، *مكتمل*.

الهدف من المشروع بناء أداة خفيفة قابلة للاستخدام الفعلي، تعمل على نطاق حقيقي ببيانات معزولة لكل مستخدم.

---

## ٢. المشكلة والدافع

المطوّر المبتدئ يشتغل عادةً على عدة مشاريع صغيرة بالتوازي (مشروع تخرج، موقع شخصي، مسار مذاكرة شهادة). أدوات إدارة المهام الاحترافية مثل Jira ثقيلة ومعقّدة لهذا الحجم، والقوائم الورقية أو ملاحظات الهاتف تفتقد الهيكلة والتتبّع.

الحاجة: أداة خفيفة تجيب على سؤالين بنظرة واحدة — **وش الشغل الحالي؟** و **كم أنجزت؟**

---

## ٣. أهداف المشروع

### أهداف المنتج
- إدارة كاملة للمشاريع والمهام (إنشاء، تعديل، حذف)
- عرض بصري واضح لحالة كل مشروع ونسبة إنجازه
- عزل تام لبيانات كل مستخدم

### أهداف تقنية
| المتطلَّب | أين يظهر في المشروع |
|---|---|
| نمذجة قواعد البيانات | علاقة `1:N` بين المشاريع والمهام |
| Migrations | كل تغيير في الـ Schema عبر ملف مُرقَّم في Git |
| أمن قواعد البيانات | سياسات RLS عبر جدولين |
| تصميم REST API | مسارات CRUD كاملة مع رموز حالة صحيحة |
| الاستعلامات المجمّعة | حساب نسبة الإنجاز بـ `COUNT` و `GROUP BY` |
| النشر | بيئة إنتاج حقيقية على نطاق مملوك |

---

## ٤. المستخدم المستهدف

**الشريحة الأساسية:** طلاب ومطوّرون مبتدئون يديرون بين ٢ و ٦ مشاريع شخصية بالتوازي.

**السياق:** استخدام فردي، لا فرق ولا مشاركة في الإصدار الأول.

**التوقّع الأساسي:** أن يفتح التطبيق ويفهم حالة مشاريعه خلال ثوانٍ، بدون تدريب ولا إعداد.

---

## ٥. نطاق الإصدار الأول

### داخل النطاق ✅
- تسجيل حساب وتسجيل دخول بالبريد وكلمة المرور
- إنشاء مشروع وتعديله وحذفه
- إنشاء مهمة وتعديلها وحذفها ونقلها بين الحالات
- تاريخ استحقاق اختياري لكل مهمة
- ترتيب المهام داخل العمود الواحد
- شريط تقدّم محسوب لكل مشروع
- تمييز بصري للمهام المتأخرة

### خارج النطاق ❌
- مشاركة المشاريع بين مستخدمين
- التعليقات والمرفقات
- الإشعارات والتنبيهات بالبريد
- تطبيق جوال أصلي
- سجل النشاط (Activity log)
- تسجيل الدخول عبر Google أو GitHub

---

## ٦. نموذج البيانات

### جدول `projects`

| العمود | النوع | القيود |
|---|---|---|
| `id` | uuid | PK، يُولَّد تلقائياً |
| `user_id` | uuid | FK → `auth.users`، NOT NULL |
| `name` | text | NOT NULL |
| `description` | text | Nullable |
| `created_at` | timestamptz | NOT NULL، افتراضي `now()` |

### جدول `tasks`

| العمود | النوع | القيود |
|---|---|---|
| `id` | uuid | PK، يُولَّد تلقائياً |
| `project_id` | uuid | FK → `projects.id`، NOT NULL، `ON DELETE CASCADE` |
| `title` | text | NOT NULL |
| `status` | text | NOT NULL، افتراضي `'todo'`، مقيَّد بـ CHECK |
| `position` | integer | NOT NULL، ترتيب داخل العمود |
| `due_date` | date | Nullable |
| `created_at` | timestamptz | NOT NULL، افتراضي `now()` |

### القيم المسموحة لـ `status`
```
'todo' | 'in_progress' | 'done'
```
مفروضة عبر `CHECK` constraint على مستوى قاعدة البيانات، لا في الكود فقط.

### قرارات تصميمية مقصودة

**١. لا نخزّن حالة التأخير.** المهمة المتأخرة تُحسب لحظة العرض:
```sql
due_date < CURRENT_DATE AND status <> 'done'
```
تخزينها كعمود يعني نسختين من الحقيقة، وإحداهما ستكذب.

**٢. لا نخزّن نسبة الإنجاز.** تُحسب باستعلام مجمّع عند طلب قائمة المشاريع.

**٣. الترتيب بأرقام متباعدة.** قيم `position` تبدأ بـ `10, 20, 30`. الإدراج بين مهمّتين يأخذ القيمة الوسطى (`15`) بتحديث صف واحد فقط، بدل إعادة ترقيم كل الصفوف.

**٤. الحذف المتتالي.** حذف مشروع يحذف مهامه تلقائياً عبر `ON DELETE CASCADE`، فلا تبقى صفوف يتيمة.

---

## ٧. الأمان والصلاحيات

### سياسات RLS

الـ Row Level Security مُفعَّل على الجدولين، وهو خط الدفاع الأساسي — لا يُعتمد على منطق التطبيق وحده.

**على `projects`:** يُسمح بالقراءة والكتابة فقط للصفوف التي `user_id` فيها يساوي المستخدم الحالي.

**على `tasks`:** الحماية غير مباشرة — تتحقق السياسة من ملكية *المشروع الأب* عبر استعلام فرعي. مهمة لا تنتمي لمشروع يملكه المستخدم غير مرئية له إطلاقاً.

### متطلبات أمنية إضافية
- المفاتيح والأسرار في `.env` المحجوب بـ `.gitignore`، ولا تُرفع أبداً لـ GitHub
- جميع الاستعلامات مُمرَّرة بمعاملات (Parameterized) — ممنوع دمج مدخلات المستخدم في نص SQL
- HTTPS إلزامي في بيئة الإنتاج
- التحقق من صحة المدخلات في طبقة الـ API قبل الوصول لقاعدة البيانات

---

## ٨. واجهة البرمجة (API)

جميع المسارات تحت `/api`، وتتطلب جلسة مصادقة صالحة.

### المشاريع

| الطريقة | المسار | الوظيفة |
|---|---|---|
| `GET` | `/api/projects` | قائمة مشاريع المستخدم مع عدّاد المهام ونسبة الإنجاز |
| `POST` | `/api/projects` | إنشاء مشروع جديد |
| `GET` | `/api/projects/:id` | تفاصيل مشروع واحد مع مهامه |
| `PATCH` | `/api/projects/:id` | تعديل الاسم أو الوصف |
| `DELETE` | `/api/projects/:id` | حذف المشروع ومهامه |

### المهام

| الطريقة | المسار | الوظيفة |
|---|---|---|
| `POST` | `/api/tasks` | إنشاء مهمة داخل مشروع |
| `PATCH` | `/api/tasks/:id` | تعديل العنوان أو الحالة أو التاريخ أو الترتيب |
| `DELETE` | `/api/tasks/:id` | حذف مهمة |

### رموز الحالة المتوقّعة

| الرمز | متى |
|---|---|
| `200` | نجاح العملية |
| `201` | إنشاء مورد جديد |
| `400` | مدخلات غير صالحة |
| `401` | لا توجد جلسة مصادقة |
| `403` | المورد موجود لكنه ليس ملكاً للمستخدم |
| `404` | المورد غير موجود |
| `500` | خطأ غير متوقّع في الخادم |

---

## ٩. الشاشات

| # | الشاشة | المحتوى |
|---|---|---|
| ١ | تسجيل الدخول | بريد + كلمة مرور، ورابط لإنشاء حساب |
| ٢ | إنشاء حساب | بريد + كلمة مرور + تأكيدها |
| ٣ | قائمة المشاريع | بطاقات المشاريع مع شريط تقدّم، وزر إضافة |
| ٤ | لوحة المشروع | ثلاثة أعمدة ببطاقات المهام، وزر إضافة مهمة |
| ٥ | نافذة المهمة | عنوان + تاريخ استحقاق + حالة (إنشاء وتعديل) |

### قواعد العرض
- المهمة المتأخرة تظهر بلون تحذيري
- المهمة المستحقة اليوم تظهر بلون معلوماتي
- المشروع بلا مهام يعرض حالة فارغة بدعوة لإضافة أول مهمة
- كل عملية حذف تتطلب تأكيداً

---

## ١٠. التقنيات

| الطبقة | الاختيار | السبب |
|---|---|---|
| الواجهة والـ API | Next.js (App Router) | لغة واحدة، ومشروع واحد، ولا مشكلة CORS |
| قاعدة البيانات | Supabase (PostgreSQL) | Postgres مُدار مع مصادقة و RLS جاهزين |
| المصادقة | Supabase Auth | يوفّر بناء نظام مصادقة من الصفر |
| إدارة الـ Schema | Supabase CLI | migrations مُرقّمة ومحفوظة في Git |
| النشر | Cloudflare | نفس مزوّد النطاق، وربط مباشر بـ GitHub |
| النطاق | Cloudflare Registrar | بسعر التكلفة، وربط DNS تلقائي |

**قرار معماري:** الـ API على مسار `/api` داخل نفس النطاق، وليس على نطاق فرعي منفصل. هذا يجعل الواجهة والـ API على **أصل واحد**، فتنتفي الحاجة لضبط CORS كلياً.

---

## ١١. مراحل التنفيذ

| المرحلة | المخرَج | معيار الاكتمال |
|---|---|---|
| **٠ — الإعداد** | مشروع Next.js مربوط بـ GitHub و Supabase | التطبيق يعمل محلياً، و `.env` محجوب |
| **١ — قاعدة البيانات** | الجدولان عبر migration في Git | الجداول موجودة، والـ migration قابلة لإعادة التطبيق |
| **٢ — المصادقة و RLS** | تسجيل ودخول + سياسات الحماية | مستخدم لا يرى بيانات مستخدم آخر — مُختبَر فعلياً بحسابين |
| **٣ — الـ API** | مسارات CRUD كاملة | كل مسار مُختبَر في Postman ويعيد الرمز الصحيح |
| **٤ — الواجهة** | الشاشات الخمس | الدورة الكاملة تعمل: إنشاء مشروع ← مهمة ← نقلها ← اكتمال |
| **٥ — النشر** | التطبيق حيّ على النطاق | يفتح عبر HTTPS، والبيانات تُحفظ في الإنتاج |

**قاعدة العمل:** كل مرحلة تنتهي بـ commit ودفع إلى GitHub. لا انتقال لمرحلة قبل اكتمال معيار السابقة.

---

## ١٢. معايير النجاح

المشروع ناجح إذا تحقّقت هذه الشروط مجتمعة:

- [ ] التطبيق يعمل على نطاق حقيقي عبر HTTPS
- [ ] حسابان مختلفان لا يريان بيانات بعضهما — مُثبَت بالتجربة
- [ ] كل تغيير في الـ Schema موجود كملف migration في Git
- [ ] لا يوجد أي مفتاح أو سر في تاريخ المستودع
- [ ] الدورة الكاملة تعمل من التسجيل حتى إكمال مهمة
- [ ] المستودع يحتوي `README` يشرح التشغيل المحلي

---

## ١٣. مخاطر معروفة

| الخطر | الأثر | التخفيف |
|---|---|---|
| نسيان تفعيل RLS | تسريب بيانات كل المستخدمين | اختبار إلزامي بحسابين قبل النشر |
| تسريب مفتاح في commit | اختراق قاعدة البيانات | `.gitignore` أولاً، وإلغاء المفتاح فوراً عند التسريب |
| فشل الترتيب عند النقل السريع | ترتيب غير متوقّع للمهام | أرقام متباعدة + تحديث صف واحد لكل نقلة |
| اختلاف متغيّرات البيئة بين المحلي والإنتاج | التطبيق يعمل محلياً ويفشل منشوراً | توثيق المتغيّرات في `.env.example` |

---

*وثيقة متطلبات — أغسطس ٢٠٢٦*
